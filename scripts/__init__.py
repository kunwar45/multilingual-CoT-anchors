# ABOUTME: Marks scripts/ as a package so stage drivers run as modules: python -m scripts.<track>.<stage>.
# ABOUTME: Running via -m from the repo root puts the root on sys.path so src.* imports work without hacks.
