# ABOUTME: Package marker for the logprob_pivots track library (secondary track): pivot scores, segmentation, prompts, config.
# ABOUTME: Import as src.logprob_pivots.<module> from the repo root (scripts run via python -m ensure sys.path is right).
"""
logprob_pivots track library — the SECONDARY, exploratory track; the primary
experiment track is src.rollout_importance. Pivot scores, sentence segmentation,
prompts, and the frozen experiment config. Import as `src.logprob_pivots.<module>`
(e.g. `from src.logprob_pivots.experiment_config import Config`) with the
project root on `sys.path` — running scripts via `python -m` from the repo
root guarantees that.
"""


