# ABOUTME: Marks src/ as a package so verified library code imports as src.<track>.<module> from the repo root.
# ABOUTME: src/ holds correctness-critical reusable code; scripts/ pipes it together, scratch/ is unverified.
